info:
  name: Search Products
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/products/search"
  params:
    - name: q
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/products/search"
      method: GET
      params:
        - name: q
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
