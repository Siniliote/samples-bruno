info:
  name: Revenue Stats
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/stats/revenue"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/stats/revenue"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
