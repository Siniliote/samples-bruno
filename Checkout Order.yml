info:
  name: Checkout Order
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/orders"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
