info:
  name: Health Check
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/api/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/health"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
