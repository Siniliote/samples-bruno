info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/users"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Created
    request:
      url: "{{baseUrl}}/api/users"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 201
      statusText: Created
      headers:
        - name: access-control-allow-credentials
          value: "true"
        - name: vary
          value: "*"
        - name: access-control-allow-origin
          value: "*"
        - name: access-control-allow-methods
          value: POST
        - name: access-control-allow-headers
          value: accept, content-type, user-agent, content-length, accept-encoding, host, connection, request-start-time
        - name: access-control-expose-headers
          value: accept, content-type, user-agent, content-length, accept-encoding, host, connection, request-start-time
        - name: content-type
          value: application/json;charset=utf-8
        - name: date
          value: Sat, 15 Aug 2026 15:47:49 GMT
        - name: content-length
          value: "46"
      body:
        type: json
        data: |-
          {
            "id": 3,
            "message": "User created successfully"
          }
